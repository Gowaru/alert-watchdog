/**
 * @function saveAlert
 * Enregistre une alerte dans Redis via une liste (Queue).
 * @param alertData - Les données de l'alerte à enregistrer.
 * @returns {Promise<void>}
 **/
export declare function saveAlert(alertData: any): Promise<void>;
